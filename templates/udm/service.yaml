apiVersion: v1
kind: Service
metadata:
  name: udm
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: free5gc-udm
  ports:
    - name: sbi
      port: 8000
      targetPort: 8000
      protocol: TCP
