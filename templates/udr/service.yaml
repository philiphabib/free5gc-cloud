apiVersion: v1
kind: Service
metadata:
  name: udr
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: free5gc-udr
  ports:
    - name: sbi
      port: 8000
      targetPort: 8000
      protocol: TCP
