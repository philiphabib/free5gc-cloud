apiVersion: v1
kind: Service
metadata:
  name: smf
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: free5gc-smf
  ports:
    - name: sbi
      port: 8000
      targetPort: 8000
      protocol: TCP
